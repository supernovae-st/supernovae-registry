name: "@mcp/fetch"
version: "1.0.0"
type: mcp
description: "Fetch web content and convert to markdown for LLM consumption"
author: "anthropic"
license: "MIT"
homepage: "https://github.com/modelcontextprotocol/servers"
tags: ["fetch", "web", "http", "scraping", "markdown"]

source:
  type: npm
  package: "@modelcontextprotocol/server-fetch"
  version: "^0.6.0"

mcp:
  tools:
    - name: fetch
      description: "Fetch URL and convert to markdown"
